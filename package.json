{
  "name": "tool-rental-provisioner",
  "version": "1.0.0",
  "private": true,
  "description": "CLI tool for provisioning NFC JavaCards via ACR122U",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "provision": "ts-node src/index.ts provision",
    "read-key": "ts-node src/index.ts read-key"
  },
  "dependencies": {
    "@noble/curves": "^1.4.0",
    "commander": "^12.0.0",
    "ethers": "^6.13.0",
    "nfc-pcsc": "^0.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.5.0"
  },
  "overrides": {
    "@pokusew/pcsclite": "latest"
  }
}
